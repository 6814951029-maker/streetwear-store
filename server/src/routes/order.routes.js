const express=require("express");const auth=require("../middlewares/authMiddleware");const admin=require("../middlewares/adminMiddleware");const c=require("../controllers/order.controller");const router=express.Router();router.post("/",auth,c.createOrder);router.get("/my",auth,c.getMyOrders);router.get("/",auth,admin,c.getOrders);router.put("/:id",auth,admin,c.updateOrder);module.exports=router;
